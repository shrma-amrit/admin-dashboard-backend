{
  "name": "admin-dashboard-backend",
  "env": {
    "MONGODB_URI": {
      "description": "Connection string to connect to MongoDB database",
      "required": true
    },
    "NODE_ENV": {
      "description": "Node environment",
      "required": true
    },
    "NODE_CONFIGENV": {
      "description": "Configuration environment",
      "required": true
    }
  }
}
